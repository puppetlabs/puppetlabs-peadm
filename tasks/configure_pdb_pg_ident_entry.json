{
  "description": "Configure a PDB PG ident entry for a single certname",
  "parameters": {
    "certname": {
      "type": "String[1]",
      "description": "The certname to authorize to connect to PuppetDB"
    },
  "input_method": "environment",
  "implementations": [
    {"name": "configure_pdb_pg_ident_entry.pp"}
  ]
}
