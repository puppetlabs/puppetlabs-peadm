{
  "description": "Run Puppet Enterprise against a identified master",
  "parameters": {
    "debug": {
      "type": "Optional[Boolean]",
      "description": "Run script in debug mode to output more information"
    },
    "puppet_master": {
      "type": "String",
      "description": "The puppet master to run puppet against"
    }
  },
  "input_method": "environment",
  "implementations": [
    {"name": "run_puppet_w_master.rb"}
  ]
}
