{
    "description": "Migrates Puppet Agents on Linux/Unix to a new Puppet Enterprise installation",
    "input_method": "environment",
    "private": true,
    "parameters":{
        "regenerate" : {
            "description": "Delete & regenerate the node's certificate? Enable this when you're not migrating the Puppet CA",
            "type": "Boolean",
            "default": true
          },
        "target_pe" : {
            "description": "FQDN of the target Puppet Enterprise server to point the agent to",
            "type": "String[1]"
        }
    }
}