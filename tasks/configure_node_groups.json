{
  "description": "Configure console node groups for a new install",
  "parameters": {
    "primary_master_host": {
      "type": "String",
      "description": "The certname of the primary master"
    },
    "r10k_sources": {
      "description": "The r10k control repo sources"
    },
    "compile_master_hosts": {
      "description": "The certname of all the compile masters"
    },
    "load_balancer_host": {
      "type": "Optional['String']",
      "description": "The certname of the load balancer"
    }
  },
  "input_method": "environment",
  "implementations": [
    {"name": "configure_node_groups.pp"}
  ]
}
